# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules

# ── Build output ─────────────────────────────────────────────────────────────
.next
.next-*
out

# ── Vercel deployment artefacts ───────────────────────────────────────────────
.vercel

# ── Env files (NEXT_PUBLIC_* is injected via --build-arg, never baked from here)
.env.local
.env.*
!.env.example

# ── Test & coverage ───────────────────────────────────────────────────────────
coverage
e2e/.auth
e2e/.results
e2e/.report
*.test.ts
*.test.tsx
*.spec.ts

# ── Docs & tooling ────────────────────────────────────────────────────────────
*.md
*.log

# ── VCS & IDE ─────────────────────────────────────────────────────────────────
.git
.gitignore
.idea
.vscode
